---
---

<section class="cta">
  <div class="cta-content">
    <h2>Ready to Transform Your Business?</h2>
    <p>Join hundreds of companies already using TechNest to accelerate their digital transformation journey.</p>
    <a href="/smart-start" class="cta-button">Start Your Smart Start</a>
  </div>
</section>

<style>
  .cta {
    padding: 5rem 2rem;
    background: linear-gradient(135deg, var(--secondary) 0%, rgba(59, 130, 246, 0.1) 100%);
    border-top: 1px solid rgba(59, 130, 246, 0.1);
    border-bottom: 1px solid rgba(59, 130, 246, 0.1);
  }

  .cta-content {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
  }

  .cta-content h2 {
    font-size: 2.25rem;
    margin: 0 0 1rem 0;
    color: var(--text-primary);
  }

  .cta-content p {
    font-size: 1.1rem;
    color: var(--text-secondary);
    margin: 0 0 2rem 0;
    line-height: 1.6;
  }

  .cta-button {
    display: inline-block;
    padding: 1rem 2.5rem;
    background: linear-gradient(135deg, var(--accent) 0%, var(--accent-light) 100%);
    color: white;
    font-weight: 600;
    border-radius: 0.5rem;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
    font-size: 1rem;
  }

  .cta-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(59, 130, 246, 0.3);
  }

  @media (max-width: 768px) {
    .cta {
      padding: 3rem 1rem;
    }

    .cta-content h2 {
      font-size: 1.75rem;
    }

    .cta-content p {
      font-size: 1rem;
    }
  }
</style>
